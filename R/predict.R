#' Binary or probabilities prediction
#'
#' @param model the model created with fit function logistic regression
#' @param new_data the new data to be classified
#' @param type_pred the type of prediction to perform (binary prediction or probability prediction)
#'
#' @return an instance containing the binary predictions, probability prediction, metrics.
#' @export
#'
#' @examples
#' \dontrun{
#'  predict(model, new_data, type_pred)
#' }
predict <- function(model, new_data, type_pred){

  # verifier que toutes les variables du modéle y figure
  # Type de prediction: probas d'affetation ou soit prediction binaire

}
